# ── Vault / notes ─────────────────────────────────────────────
jarvis_vault/
brain/__pycache__/

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/

# ── Node ──────────────────────────────────────────────────────
node_modules/
frontend/node_modules/

# ── OS / editor / process ─────────────────────────────────────
.DS_Store
.pids/
*.log
logs/
audio/

# ── SECRETS — never commit ────────────────────────────────────
.env
.env.*
.jarvis_secret_key
*.key
auth_secret
workflow_secrets.json
credentials.json
tenants.json

# ── Runtime data stores (PII / state; app regenerates) ────────
agent_memory.sqlite
agent_memory.sqlite-journal
*.sqlite
*.sqlite-journal
agent_chats.json
cdp_events.jsonl
cdp_profiles.json
cdp_identity_graph.json
cdp_segments.json
cdp_destinations.json
cdp_journeys.json
orchestrator_runs.json
orchestrator_agents.json
ours_landing.jsonl
ours_connector_state.json

# ── Backups / build artifacts ─────────────────────────────────
_backups/
*.bak
*.bak.*
_logo_preview.png
*.png.tmp

data/jarvis.db
data/usage_log.jsonl
cdp_journey_runs.json
workflow_runs.json
semantic/jobs.json
datavant_assets.json

# TLS material — self-signed localhost dev certs, never commit
*.pem
*.crt
*.cer
*.p12
*.pfx
*.pem.*

# --- Local runtime state / personal data — never publish (added for GitHub safety) ---
convo_state/
convo_artifacts/
brief_store.json
brief_store.json.bak*
phase2_evidence.md
martin_app.html.bak.*
*.bak.*
.env.bak.*

# --- Secret/config with provider keys — never publish ---
llm_providers.json
# --- Local brief config (may hold Outlook content) ---
brief_in.json
brief_mute.json
# --- Embedded subprojects / heavy demo assets (not core) ---
higgsfield-studio/
prestige-os/
prestige-os.zip
prestige-demo/
prestige-demo.zip
*.zip
generated_reports/
